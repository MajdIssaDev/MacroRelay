name: macro_relay
description: MacroRelay — record and play Windows macros with a native C++ engine.
publish_to: "none"
version: 1.3.2+8

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.4
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
